# Minimal keyless composition for loading the mcp-servers registry connector
# against the package-owned fixture server in mcp-servers.e2e.ts. Source
# builtins keep this unit test independent of prebuilt workspace artifacts.
- id: system-prompt
  name: cordis:mcp-servers-test-system-prompt

- id: tools
  name: cordis:mcp-servers-test-tools

- id: mcp-servers
  name: cordis:mcp-servers-test
