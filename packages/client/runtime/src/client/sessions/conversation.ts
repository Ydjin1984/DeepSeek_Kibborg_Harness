// Conversation projection types and helpers live in contract (the outward
// snapshot face); this module re-exports them for in-domain consumers.
export * from '../contract/conversation-snapshot.ts'
